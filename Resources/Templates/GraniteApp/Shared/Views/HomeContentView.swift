import SwiftUI

struct HomeContentView: View {
    var body: some View {
        ZStack {
            Color.clear
            GraniteLogoView()
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
